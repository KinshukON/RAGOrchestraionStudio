# Python dependencies for the Vercel Serverless Function at api/index.py
# Vercel reads this file from the api/ directory to install function-level dependencies.

# ── Mangum: ASGI → Lambda adapter (required for Vercel Python Functions) ─────
mangum>=0.17.0

# ── Core backend deps ─────────────────────────────────────────────────────────
fastapi==0.115.0
sqlmodel==0.0.37
sqlalchemy==2.0.48
psycopg2-binary==2.9.10
requests==2.32.3
PyJWT==2.9.0
alembic>=1.13.0
httpx==0.27.0

# ── RAG connectors ────────────────────────────────────────────────────────────
openai>=1.30.0
anthropic>=0.28.0
cohere>=5.5.0
asyncpg>=0.29.0
neo4j>=5.20.0
python-dotenv>=1.0.0
